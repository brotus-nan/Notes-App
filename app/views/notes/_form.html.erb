<%= simple_form_for note do |form| %>

  <%= form.input :title %>

  <%= form.input :content, as: :text %>

  <%= form.association :user, collection: User.all, label_method: :name, value_method: :id, include_blank: 'Select User' %>

  <%= form.button :submit %>

<% end %>